mapper-
	mapper(<row ..../>) ---> (month, len(text))
reucer-
	reducer(month, List[len(Text), len(Text),....])---> (month, len(List),median(List))







By analysing the graph it shows that when i plot year-month vs median length from May 2014 to Sept 2021 it shows almost uniform distribution